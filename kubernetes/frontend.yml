apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: wanderlust
spec:
  replicas: 1
  selector:
    matchLables:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend 
    spec:
      containers: 
        - name: frontend
          image: harshalg01/wanderlust-web:v2
          ports:
            - containerPort: 5173
          env: 

---

apiVersion: v1
kind: Service 
metadata:
  name: frontend-svc
  namespace: wanderlust
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 5173
      targetPort: 5173
      protocol: TCP
      nodePort: 31000